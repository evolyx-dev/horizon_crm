# ─────────────────────────────────────────────────────────
# Horizon CRM — Production Environment Variables
# Copy to .env and fill in values before deploying
# ─────────────────────────────────────────────────────────

# ── Database (external MySQL / MariaDB) ──────────────────
DB_HOST=mariadb
DB_PORT=3306
DB_ROOT_PASSWORD=changeme_db_root_password

# ── Frappe Runtime Pin ───────────────────────────────────
FRAPPE_BENCH_IMAGE=frappe/bench@sha256:9c79fb8acd3e23a11e62aa6771f3426909303fddee39785adf645a0f2df7d208
FRAPPE_BRANCH=develop
FRAPPE_COMMIT=0afbf2a98e00288bba20a3a6231b7dabe31df69f

# ── Frappe Site ──────────────────────────────────────────
PRIMARY_SITE_NAME=horizon.localhost
SECONDARY_SITE_NAME=tenant2.localhost
ADMIN_PASSWORD=changeme_admin_password

# ── Redis (container hostnames — no need to change) ─────
REDIS_CACHE_HOST=redis-cache
REDIS_QUEUE_HOST=redis-queue

# ── Nginx ────────────────────────────────────────────────
HTTP_PORT=80

# ── Performance Tuning ───────────────────────────────────
# Gunicorn workers (auto-detected if blank; max 4)
GUNICORN_WORKERS=
# Worker queue name (default: default,short,long)
WORKER_QUEUE=default,short,long
